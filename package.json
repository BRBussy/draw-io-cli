{
  "name": "draw-io-cli",
  "version": "0.1.0",
  "description": "Extract and render draw.io diagrams from the command line using the draw.io webapp bundled with the hediet.vscode-drawio extension.",
  "type": "module",
  "bin": {
    "drawio-cli": "src/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node test/smoke.mjs"
  },
  "dependencies": {
    "playwright": "1.62.1"
  }
}
